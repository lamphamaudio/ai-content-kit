# Prompt Engine

`PromptService` builds prompts by generation type and prompt version.

Provider integration is behind `AIProvider` with placeholders for OpenAI, Gemini, and Claude. Without keys, the API returns mock output.

