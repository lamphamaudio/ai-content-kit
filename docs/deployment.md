# Deployment

- `apps/web`: deploy to Vercel.
- `apps/api`: deploy to Render, Fly, or Railway.
- Supabase manages PostgreSQL and Auth.

Set environment variables from `.env.example` in each target platform.

